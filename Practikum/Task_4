#Задача 4. Студент се явява на изпит с конспект от 20 въпроса. От тях не знае само 3
#въпроса. На изпита си тегли 2 въпроса от конспекта. Каква е вероятността да знае само
#един от изтеглените въпроси?

#Общо 20 въпроса 
#Не знае 3 
#Знае 20-3=17
#Ще отбележа с 0 тези които не знае и с 1 тези които знае 
sim.exam <- function(){
#Създава редица от 17 единици и 3 нули
    questions <- c(rep(1, 17), rep(0, 3))
    
    draws <- sample(questions, 2, replace = FALSE)
    sum(draws == 1)
}
res <- replicate(100000, sim.exam())
sum(res)/length(res)
