#Задача 7. На всеки от върховете на равностранен триъгълник има една мравка. Всяка
#мравка избира произволно един от другите два върха и тръгва към него. За единица време
#всякамравкаизминаваразстояниетоотединвръхдодруг.Двемравкимогатдасеразминат
#ако тръгнат една срещу друга. Каква е вероятността след единица време да има по една
#мравка на всеки връх?

#Върховете са 1,2,3
#Мравка на връх 1 може да отиде на връх 2 или 3 и така и за другите
#Всяка мравка избира произволно на кой от двата върха да отиде
sim.ants <- function()
{
#Всяка мравка си избира случайно един от върховете
    ant1 <- sample(c(2,3), 1)
    ant2 <- sample(c(1,3), 1)
    ant3 <- sample(c(1,2), 1)
    destinations <- c(ant1, ant2, ant3)
    
    #!any(duplicated(...)), което означава "няма нито един дубликат"
    !any(duplicated(destinations))
}
res <- replicate(100000, sim.ants())
sum(res) / length(res)
