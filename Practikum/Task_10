#Задача 10. Авиокомпания е продала 143 билета за самолет, в който има 138 пътнически
#места. Вероятността пътник да дойде навреме за полета си е 0.92. Нека приемем, че даден
#пътник идва навреме независимо от останалите пътниците.
#а) Каква е вероятността да има място за всички пътници, които са дошли навреме?
#б) Каква е вероятността да остане едно незаето пътническо място?

sim.airplane <- function(){
#sample ще създаде списък от 143 числа като всяко от тях е или 0 или 1 
    x <- sample(c(0,1), size = 143, replace=TRUE, prob=c(0.08, 0.92))
    sum(x)
}
res <- replicate(100000, sim.airplane())
#a)
sum(res <= 138)/length(res)
#b)
sum(res== 137)/length(res)
