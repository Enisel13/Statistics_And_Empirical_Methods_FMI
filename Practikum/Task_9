#Задача 9. На студенти е даден тест от 10 въпроса, всеки с по 4 възможни отговора, един
#от които е верен. Иван се явява на теста без да е учил и огражда произволно отговори.
#Каква е вероятността да е отговорил вярно на поне 5 от въпросите?

#Има вероятнос за успех 1/4. Представяме това като за всеки въпрос теглим 
#число от 1 до 4 и ако числото е 1 приемаме че е отговорил правилно
sim.test <- function(){
    answers <- sample(1:4, size=10, replace=TRUE)
    
    correctCount <- sum(answers == 1)
    correctCount >= 5
}
res <- replicate(100000, sim.test())
sum(res) / length(res)
