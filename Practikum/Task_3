#Задача 3. Иван има 4 ключа, но не знае кой е за неговата стая. Предполагаме, че клю-
#човете са разбъркани по произволен начин. Иван пробва последователно с всеки от тях,
#като помни кой ключ е пробвал. Каква е вероятността да отключи с последния (четвъртия)
#ключ?

sim.keys <- function(){
#онзчаваме верния ключ с номер едно, а другите 2,3,4 са грешни
    keys <- c(1:4)
    x <- sample(keys, 4, replace = FALSE)
    x[4] == 1 
}
res <- replicate(100000, sim.keys())
sum(res)/length(res)
